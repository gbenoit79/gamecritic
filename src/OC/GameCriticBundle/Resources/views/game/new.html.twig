{% extends '@OCGameCritic/layout.html.twig' %}

{% block body %}
    <h1>Game creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('oc_game_critic_homepage') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
